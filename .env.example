# Comma-separated email addresses allowed to use the editor.
# Set this in production. When omitted, any signed-in visitor can use /admin.
ADMIN_EMAILS=curator@example.com

# Absolute deployed origin used to build Open Graph image URLs.
NEXT_PUBLIC_SITE_URL=https://your-site.example
